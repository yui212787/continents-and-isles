# 《大陆与群岛》中文开发速查手册

> 版本：1.0.1 | Minecraft：1.21.1 | NeoForge

---

## 一、项目总览

**Continents and Isles** 是一个世界生成模组：创建「中央超大陆 + 外围深海散岛」的巨型世界结构。超大陆内部分 6 个主题扇区（山脉/丛林/群岛/沙漠/热带草原/雪原），扇区之间由大尺度气候群系过渡；大陆核心有 3 个必生成大湖（岛湖/深湖天池/群系湖），大陆边缘被环形高山环绕，外侧断崖入海。所有地形与群系由自定义密度函数（Density Function）和自定义群系源（Biome Source）协作生成，保证两者严格对齐。

### 目录职责表

| 路径 | 职责 |
|---|---|
| `src/main/java/.../ContinentsAndIslesMod.java` | 模组入口：注册所有密度函数/群系源/结构/特征类型，监听服务器启动事件并初始化场 |
| `src/main/java/.../CAIConfig.java` | 模组配置：`config/continents_and_isles.toml` 映射，含大陆半径、扇区群系、湖开关等 |
| `src/main/java/.../ContinentIslandField.java` | **核心场计算**：大陆度偏置、扇区、湖、群岛、噪声等所有空间计算的静态工具箱 |
| `src/main/java/.../RadialLand.java` | 密度函数（替代原版 continents）：输出大陆度，驱动整体地形高低 |
| `src/main/java/.../ContinentsAndIslesBiomeSource.java` | 群系源：按优先级逐级判定（府邸 → 湖 → 湿地 → 扇区 → 大陆 → 外岛 → 原版） |
| `src/main/java/.../ArchipelagoTransition.java` | 密度函数：群岛扇区内缘过渡带（沙滩→浅海→深海）的地形压低 band |
| `src/main/java/.../ArchipelagoWetland.java` | 密度函数：群岛-环山带过渡浅滩（沼泽/红树林）的地形压低 band |
| `src/main/java/.../ArchipelagoErosionSuppress.java` | 密度函数：群岛扇区内禁止原版侵蚀（erosion）把小岛切烂 |
| `src/main/java/.../MountainSector.java` | 密度函数：山脉扇区 8 条弧线同心山脉的结构抬升 |
| `src/main/java/.../RingMountain.java` | 密度函数：大陆边缘环形高山的结构抬升 |
| `src/main/java/.../TianchiMountain.java` | 密度函数：山脉扇区湖的"天池"环形山抬升 |
| `src/main/java/.../DeepLake.java` | 密度函数：山脉扇区深湖的 V 型盆底挖掘 |
| `src/main/java/.../DeepLakeSuppress.java` | 密度函数：抑制山脉弧线在深湖/天池区域的抬升（避免盆底冒山） |
| `src/main/java/.../LakeBasin.java` | 密度函数：3 个必生成大湖的湖盆统一雕刻 |
| `src/main/java/.../LakeBiomeLowland.java` | 密度函数：群系湖（湖2）湖面压低 band（LERP 模式，湖面中心绝对 -0.50） |
| `src/main/java/.../IslandLowland.java` | 密度函数：群岛扇区内抑制山脉弧线的抬升，保持低地 |
| `src/main/java/.../RingMountainOreFeature.java` | 矿物特征：环形山脉专用的矿物生成（只在环山带内放置，深度/密度特殊） |
| `src/main/java/.../ContinentsIslandMonumentStructure.java` | 结构：海洋神殿（固定在群岛扇区，半径 300 格清空岛屿） |
| `src/main/java/.../ContinentsIslandDesertPyramidStructure.java` | 结构：沙漠神庙（固定在沙漠扇区环带） |
| `src/main/java/.../ContinentsIslandSwampHutStructure.java` | 结构：沼泽小屋（固定在湿地浅滩带） |
| `src/main/java/.../ContinentsIslandWoodlandMansionStructure.java` | 结构：林地府邸（固定在扇区间隙，周围强制黑森林） |
| `src/main/resources/data/.../density_function/overworld/*.json` | 密度函数 JSON 组合层：用加/乘/lerp 组装 Java 密度函数成最终 offset/factor 链 |
| `src/main/resources/data/.../world_preset/continents_and_isles.json` | 世界预设入口 |
| `src/main/resources/data/.../noise_settings/overworld.json` | 噪声设置（海平面 63 等基础） |

---

## 二、初始化与事件时序

```
RegisterEvent 阶段                ServerAboutToStart 阶段          世界生成中
─────────────────────────         ──────────────────────         ──────────
ContinentsAndIslesMod             ContinentIslandField             首次密度函数调用
  ↓ registerWorldgenTypes()          ↓ loadConfig()                  ↓ ensureConfigLoaded()
  ├ DENSITY_FUNCTION_TYPE            ├ 从 CAIConfig 读取到静态变量    ↓ 保证静态变量已刷新
  ├ BIOME_SOURCE                     └ initSectorRotation(seed)      ↓ 第一次 getNoiseBiome()
  ├ STRUCTURE_TYPE                     ↓ 扇区旋转角 + 世界种子混合    ↓ ensureConfig()
  └ FEATURE                            ↓ initLake (3个大湖)            ↓ 解析 SectorBiomeData
                                       ↓ 神殿/神庙/府邸坐标
                                       ↓ 蘑菇岛单元格
```

【注意】注册期早于配置加载，所有密度函数构造函数中的 `radius` / `grid` 等参数**仅用于 Codec 编解码**，真实值必须在 `compute()` 中从 `ContinentIslandField.ensureConfigLoaded()` 后的静态变量读取。

---

## 三、逐文件说明

### 3.1 ContinentsAndIslesMod.java — 模组入口

**一句话职责**：注册所有世界生成类型，监听服务器启动事件并初始化 `ContinentIslandField` 的配置与种子相关状态。

| 中文名 | 原始英文名 | 作用 | 输入 | 输出 |
|---|---|---|---|---|
| 模组常量 ID | `MOD_ID` | 命名空间常量，注册资源定位符用 | — | `"continents_and_isles"` |
| 服务器启动回调 | `onServerAboutToStart()` | 获取世界种子，先 `loadConfig()` 再 `initSectorRotation()` | 事件对象（含种子） | void |
| 世界生成类型注册 | `registerWorldgenTypes()` | 在 `RegisterEvent` 中注册 12 种密度函数、1 种群系源、4 种结构、1 种特征 | 事件对象 | void |

---

### 3.2 CAIConfig.java — 配置

**一句话职责**：`config/continents_and_isles.toml` 的静态映射，所有可调参数的声明与默认值来源。

| 配置项 | 含义 | 默认值 | 范围 | 读取到的静态变量 |
|---|---|---|---|---|
| `radius` | 超大陆半径（格） | 4250 | 500~20000 | `continentRadius` |
| `transition` | 海岸过渡带宽（格） | 160 | 50~5000 | `continentTransition` |
| `grid` | 外围岛屿网格单元大小（格） | 400 | 100~2000 | `continentGrid` |
| `island_chance` | 每网格单元岛屿概率 | 0.35 | 0~1 | `continentIslandChance` |
| `far_island_start_multiplier` | 远海区起点（R 的倍数） | 2.5 | 1~50 | `farIslandStartMul` |
| `far_island_chance_multiplier` | 远海岛屿概率倍率 | 1.35 | 0~3 | `farIslandMul` |
| `lake_east_enabled` | 岛湖开关 | true | bool | `lakeEnabled[0]` |
| `lake_mountain_enabled` | 山脉深湖/天池开关 | true | bool | `lakeEnabled[1]` |
| `lake_biome_enabled` | 群系湖开关 | true | bool | `lakeEnabled[2]` |
| `lake_center_fraction` | 岛湖中心位置（R 比例） | 0.19 | 0~0.5 | `lakeCenterFraction` |
| `sector_half_width` | 扇区半宽（度） | 20.0 | 5~30 | `sectorHalfWidthDeg` |
| `sector_dist_lo` | 扇区环带内径比例 | 0.30 | 0~0.8 | `sectorDistLo` |
| `sector_dist_hi` | 扇区环带外径比例 | 0.85 | 0.2~1.0 | `sectorDistHi` |
| `ring_mountain_enabled` | 环形山脉开关 | true | bool | `ringMountainEnabled` |
| `sector_N_main` | 扇区 N 主群系 | 见文件 | 资源定位符字符串 | 群系源 `sectorBiomeData[N].main` |
| `sector_N_extras` | 扇区 N 附属群系列表 | 见文件 | 字符串列表 | `sectorBiomeData[N].extras` |
| `sector_N_extra_weights` | 扇区 N 附属群系对应权重 | 见文件 | 双精度列表 | `sectorBiomeData[N].extrasCumulative` |
| `outer_island_biome_blacklist` | 外岛禁止群系列表 | 空 | 字符串列表 | `outerIslandBlacklist` |

---

### 3.3 ContinentIslandField.java — 核心空间场

**一句话职责**：所有「仅依赖 (x, z) 和配置」的空间计算静态工具箱，密度函数（地形）和群系源（群系）共享同一套结果，保证严格对齐。

#### 关键函数表

| 中文名 | 原始英文名 | 作用 | 输入 | 输出 |
|---|---|---|---|---|
| 初始化扇区旋转 | `initSectorRotation(seed)` | 由世界种子派生扇区整体旋转角 + 世界种子混合值，初始化 3 湖、蘑菇岛、三大结构坐标 | long 种子 | void |
| 加载配置 | `loadConfig()` | 从 `CAIConfig.SPEC` 读取所有配置，写入类静态变量 | — | void |
| 确保配置就绪 | `ensureConfigLoaded()` | 防御性初始化：若静态变量仍是默认注册值，重新 `loadConfig()` | — | void |
| 大陆度偏置 | `bias(x, z, cfg)` | **核心入口**：返回 -1~1 的大陆度偏置（越大越陆），优先级：湖 → 群岛扇区 → 大陆 → 外岛 | x, z, Config | double (-1~1) |
| 扇区中心角 | `sectorCenterAngle(sector)` | 返回扇区 N 的中心方位角（已按种子旋转） | int 0~5 | 弧度 |
| 扇区掩膜 | `sectorMask(sector, x, z, cfg)` | 返回扇区 N 的角度×径向联合 mask（0=外，1=中心），smoothstep 平滑 | sector, x, z, Config/radius | double 0~1 |
| 群岛扇区掩膜 | `islandSectorMask(x, z, cfg)` | 扇区 2 的 mask（角度+径向联合），群岛相关判定专用 | x, z, Config | 0~1 |
| 群岛扇区 falloff | `islandSectorFalloff(x, z, cfg)` | **群岛空间距离变量**：0=大陆，越大越靠近群岛内海，带外溢场（mask=0 时仍有值供过渡带用） | x, z, Config/radius | 0~1+ |
| 群岛内部值 | `islandSectorValue(x, z)` | 群岛内海/小岛的 bias 值（-0.65 海 → -0.9~0.6 小岛抬升） | x, z | -0.9~0.6 |
| 群岛内陆地判定 | `islandSectorIsLand(x, z)` | 群岛内部某点是否为小岛陆地（供群系源判定用） | x, z | bool |
| 湿地带 band | `archipelagoWetlandBand(x, z, radius)` | 湿地浅滩带（0.80R~0.98R）的 0~1 band，供地形+群系对齐 | x, z, R | 0~1 |
| 群岛过渡权重 | `islandTransitionWeight(x, z, cfg)` | 过渡带（falloff 0.05~0.40）的细河网权重 | x, z, Config | 0~1 |
| 湖值 | `lakeValueAt(idx, x, z, cfg)` | 第 idx 个湖的湖面偏置值（岛湖有小岛抬升，深湖 V 型底，群系湖均一），湖外 NaN | idx, x, z, Config | double 或 NaN |
| 深湖盆底 Y | `deepLakeBottomY(idx, x, z)` | 仅山脉湖：盆底深度（~Y=8 附近），供 DeepLake 密度函数雕刻 | idx, x, z | 深度因子 |
| 山脉结构值 | `mountainValue(x, z, radius)` | 山脉扇区 8 条弧线的抬升结构值（0~1.15，越高越外弧） | x, z, R | ≥0 |
| 外岛陆地判定 | `isOuterIslandLand(x, z, cfg)` | dist≥R 区域：某点是否落在外岛陆地（岛屿池判定） | x, z, Config | bool |
| 值噪声 | `valueNoise(x, z, cell, seed)` | 平滑值噪声（双线性），用于岸线曲折、湖形态扭曲等 | x, z, 网格大小, 随机种子 | 0~1 |
| 哈希 | `hash(x, z, seed)` | 确定性 0~1 哈希（不连续），用于岛屿概率、附属群系抽取等 | long x, long z, seed | 0~1 |

#### bias() 逐行业务逻辑（核心入口）

```
1. 计算到原点距离 dist
2. 若 dist < R（大陆内部）：
   ├─ 2a. 基础大陆基盘 base = 0.6 + 0.14 * valueNoise(600 网格)
   ├─ 2b. 遍历 3 个湖（优先级最高，先于群岛扇区！）：
   │    命中湖时：湖面值 shallow = lakeValueAt + 0.12*dd（dd<1 时向岸边渐浅）
   │              edge = smoothstep(dd 在 0.60~1.05 之间归一化)
   │              返回 lerp(edge, shallow, base)  — 0.60 内纯湖，1.05 外纯陆地
   ├─ 2c. 群岛扇区判定（falloff 单变量连续过渡，无 mask 跳变）：
   │    ├─ falloff <= 0.05：跳过，返回 base（纯陆地）
   │    ├─ extW = smoothstep((falloff-0.05)/0.35)：falloff 0.05→0 权重 0→1
   │    ├─ falloff <= 0.40：返回 lerp(extW, base, -0.65) （+ 叠加细河网）
   │    └─ falloff > 0.40：w2=islExt>0.20 时小岛回升，否则 -0.65 纯海
   └─ 2d. 不命中湖和群岛：返回 base
3. 若 dist < R+T（大陆→海洋过渡带）：
   └─ fall = 0.6*(1-fall) - 0.60*fall，叠 valueNoise 让岸线有曲折
4. 若 dist ≥ R+T（外围）：
   ├─ baseChance = islandChance（≥ 2.5R 时乘 farIslandMul 做远海调整）
   ├─ 3×3 邻域外岛网格单元格遍历
   ├─ 每个单元 hash(ccx, ccz, 303) < chance 时生成岛
   ├─ 岛中心偏移 ±0.6 格，半径 0.38~0.56 网格，域扭曲边缘不规则
   └─ 返回 best（-0.90 深海 或 岛屿抬升）
```

#### 调用关系图（核心流程）

```
RadialLand.compute()
  └→ ContinentIslandField.bias()  ←─────── 大陆度偏置主入口
       ├→ lakeValueAt()            [湖]
       ├→ islandSectorFalloff()    [群岛空间变量]
       ├→ islandSectorMask()       [群岛掩膜]
       ├→ islandSectorValue()      [群岛内海+小岛]
       │   └→ valueNoise()         [域扭曲/位置偏移]
       ├→ valueNoise()             [大陆基盘噪声/岸线曲折]
       └→ hash()                   [外岛概率/结构位置]

ContinentsAndIslesBiomeSource.getNoiseBiome()
  └→ 同样调用上述函数，保证群系判定位置与地形完全对齐
```

---

### 3.4 RadialLand.java — 径向大陆密度函数

**一句话职责**：替代原版 `continents` 密度函数，将 `ContinentIslandField.bias()` 包装成 Minecraft 世界生成体系可识别的密度函数。

| 中文名 | 原始英文名 | 作用 | 输入 | 输出 |
|---|---|---|---|---|
| 主计算 | `compute(ctx)` | 返回大陆度：<br>• 大陆及过渡区 = `bias + 0.10 × 原版大陆度`（保留岸线曲折）<br>• 外围纯岛屿 = `clamp(bias)`（不受原版干扰） | DensityFunction.FunctionContext | double -1~1 |

【注意】构造函数中的 `radius/transition/grid/islandChance` 仅为 Codec 占位，`compute()` 必须从 `ContinentIslandField` 静态变量读取真实值。

---

### 3.5 ContinentsAndIslesBiomeSource.java — 群系源

**一句话职责**：按优先级逐级判定每个 (x, y, z) 的群系，与 `ContinentIslandField` 同场判定保证地形-群系对齐。

#### 群系判定优先级（自上而下先命中先返回）

1. **林地府邸固定点**（周围 200 格强制黑森林）
2. **三个必生成大湖**：
   - 湖 0（岛湖）：湖面 = `ISLAND_SECTOR_OCEAN`，湖中小岛随机从 `mainlandPool` 抽取
   - 湖 1（深湖/天池）：湖面 = `ISLAND_SECTOR_OCEAN`，天池顶部群系按普通分级
   - 湖 2（群系湖）：岸边 `falloff<0.10` 为沙滩，湖面按 `lakeBiomeIndex`（10 种群系之一），岸边外陆地正常
3. **湿地浅滩带**（`archipelagoWetlandBand>0.02`）：红树林/沼泽混合，与地形 `ArchipelagoWetland` 严格对齐
4. **群岛过渡带**（群岛扇区 falloff）：
   - `falloff 0.05~0.10`：BEACH 沙滩
   - `falloff 0.10~0.20`：ISLAND_SECTOR_OCEAN 强制海洋（杜绝草地夹海）
   - `>0.20`：`islandSectorIsLand` 判定 → 小岛陆地群系 or 内海
5. **六大扇区**（0~5）：
   - 扇区 0（山脉）：按大陆度 y 值分级（山脚草地 → 林 → 雪顶/冰封峰顶）
   - 扇区 2（群岛）：与群岛扇区内部小岛逻辑一致
   - 扇区 1/3/4/5：按 `sectorMask` + `hash` 抽取主群系/附属群系（沙漠 75%沙漠+25%恶地，雪原 84% 雪原 + 冰刺 + 针叶林）
6. **超大陆普通群系**：用大尺度噪声（温度/湿度/起伏）从 `mainlandPool` 的 10 种大陆群系中选择
7. **超大陆之外**：
   - 外岛：每个岛屿单元格固定一个群系（按 hash 从 `islandPool` 抽取，禁止黑名单群系）
   - 深海：委托 `delegate`（原版多噪声群系源）给海洋群系

| 中文名 | 原始英文名 | 作用 | 输入 | 输出 |
|---|---|---|---|---|
| 主群系分配 | `getNoiseBiome(x, y, z, sampler)` | 按优先级逐级判定群系 | x,y,z, Climate.Sampler | Holder\<Biome\> |
| 允许雪 | `allowSnow(x, z, mask)` | 雪原扇区允许，其他扇区仅高山/高纬允许（防止低海拔乱下雪） | x, z, mask | bool |
| 外岛黑名单命中 | `isOuterIslandBlacklisted(biome)` | 判定某群系是否在外岛黑名单中 | Holder\<Biome\> | bool |
| 外岛兜底 | `outerIslandFallback()` | 所有 islandPool 都被黑名单时，返回森林/平原兜底 | — | Holder\<Biome\> |

---

### 3.6 ArchipelagoTransition.java — 群岛过渡带压低函数

**一句话职责**：返回 0~1 band，在 JSON 中被 `-0.50 * band` 叠加，负责把大陆→沙滩→浅海→深海的地形连续压低。

| falloff 区间 | band 值 | 实际修正 (-0.50×) | 业务效果 |
|---|---|---|---|
| ≤0.02 或 ≥0.43 | 0 | 0 | 窗口外不压低 |
| 0.02→0.05 | 0→0.10 | 0→-0.05 | 大陆→沙滩前准备 |
| 0.05→0.10 | 0.10→0.25 | -0.05→-0.125 | **沙滩带（≈30格）：弱压低，海平面附近** |
| 0.10→0.15 | 0.25→1.0 | -0.125→-0.50 | 沙滩→深海过渡，快速下潜 |
| 0.15→0.40 | 1.0 | -0.50 | 海洋主体，全压低 |
| 0.40→0.43 | 1→0 | -0.50→0 | 过渡回群岛核心，小岛开始 |

【注意】0.02~0.05 段升为 **0.10**（不是 1.0），必须与下一段沙滩带的起点 0.10 对齐，否则有地形跳变。

---

### 3.7 密度函数链（JSON 叠加层）

最终地形 offset 的叠加路径：

```
offset_mountain.json (flat_cache, 最终入口)
  └→ add
       ├→ base (offset_before_archipelago + wetland + transition)
       │    ├→ offset_before_archipelago.json (cache_2d)
       │    │    └→ add
       │    │         ├→ add: offset + (island_lowland × deep_lake_suppress × mountain_sector × 0.32)
       │    │         ├→ add: ring_mountain × 0.72
       │    │         └→ add: (deep_lake_suppress × tianchi_mountain × 0.30) + lake_basin
       │    ├→ archipelago_wetland:  band × -0.515 (湿地带压低 ~ Y≈64)
       │    └→ archipelago_transition: band × -0.50 (沙滩→海洋分级压低)
       └→ lake_biome_lowland LERP:  base × (1-band) + (-0.50) × band
                                    湖面中心绝对 -0.50，岸边形成沙滩环
```

【注意】湖的模式是 **LERP**（线性插值），不是 ADD：
- ADD 模式：`base + (-0.50 × band)` — 湖面中心深度受 base 影响（base 高则湖浅）
- LERP 模式：`base×(1-band) + (-0.50)×band` — 湖面中心**永远**是 -0.50，岸边沙滩环稳定

---

## 四、变量字典

### 4.1 业务关键变量

| 变量名 | 类型 | 含义 | 取值范围 | 在哪里修改 |
|---|---|---|---|---|
| `continentRadius` | int | 超大陆半径（格） | 默认 4250，配置 500~20000 | `loadConfig()` → `CAIConfig.RADIUS` |
| `continentTransition` | int | 大陆→海洋过渡带宽（格） | 默认 160，配置 50~5000 | `loadConfig()` → `CAIConfig.TRANSITION` |
| `continentGrid` | int | 外岛网格单元大小（格） | 默认 400 | `loadConfig()` → `CAIConfig.GRID` |
| `continentIslandChance` | double | 每网格外岛概率 | 默认 0.35，配置 0~1 | `loadConfig()` → `CAIConfig.ISLAND_CHANCE` |
| `farIslandStartMul` | double | 远海区起点（R 倍数） | 默认 2.5 ≈一万格 | `loadConfig()` → `FAR_ISLAND_START_MULTIPLIER` |
| `farIslandMul` | double | 远海岛屿概率倍率 | 默认 1.35（远海多 35%），0=远海无岛 | `loadConfig()` → `FAR_ISLAND_CHANCE_MULTIPLIER` |
| `lakeCenterFraction` | double | 岛湖中心位置（R 比例） | 默认 0.19（出生点东侧） | `loadConfig()` → `LAKE_CENTER_FRACTION` |
| `sectorHalfWidthDeg` | double | 扇区半宽（度） | 默认 20.0，配置 5~30 | `loadConfig()` → `SECTOR_HALF_WIDTH` |
| `sectorDistLo` | double | 扇区环带内径比例（×R） | 默认 0.30 ≈1275 格 | `loadConfig()` → `SECTOR_DIST_LO` |
| `sectorDistHi` | double | 扇区环带外径比例（×R） | 默认 0.85 ≈3612 格 | `loadConfig()` → `SECTOR_DIST_HI` |
| `ringMountainEnabled` | boolean | 环形山脉开关 | 默认 true | `loadConfig()` → `RING_MOUNTAIN_ENABLED` |
| `LAND_BIAS_THRESHOLD` | double | bias≥此值按陆地判定 | 固定 -0.12 | 常量（不可配置） |
| `ISLAND_SECTOR` | int | 群岛扇区索引（扇区2） | 固定 2 | 常量（与种子旋转后实际方位无关） |
| `LAKE_COUNT` | int | 必生成大湖总数 | 固定 3 | 常量 |
| `lakeType[]` | int[] | 每个湖的类型：0=群系湖, 1=深湖, 2=岛湖 | 0/1/2 | `initSectorRotation()` 按种子决定 |
| `lakeRadiusActual[]` | double[] | 每个湖的实际半径（格） | 种子随机浮动 120~260 | `initSectorRotation()` → `initLake()` |
| `lakeCX[]/lakeCZ[]` | double[] | 每个湖的中心坐标（block） | 岛湖固定，深湖在山脉扇区，群系湖随机扇区 | `initLake()` |
| `lakeTianchi[]` | boolean[] | 山脉湖是否为"天池"模式（环形山 + 湖顶） | 约 50% 概率 | `initLake()` 种子判定 |
| `lakeBiomeIndex[]` | int[] | 群系湖的群系索引（映射到 mainlandPool） | 10 种候选之一 | `initLake()` 种子抽取 |
| `lakeEnabled[]` | boolean[] | 每个湖的开关 | 配置决定 | `loadConfig()` 读 `LAKE_N_ENABLED` |
| `sectorRotation` | double | 六大扇区整体旋转角（弧度） | 0~2π 种子随机 | `initSectorRotation()` |
| `worldSeedMix` | long | 世界种子混合值，混入所有 hash/噪声 | 由种子派生 | `initSectorRotation()` |
| `MONUMENT_CLEAR_RADIUS` | double | 海洋神殿周围清空岛屿半径 | 固定 300 格 | 常量 |
| `mushroomCellX/Z` | long | 必生成蘑菇岛的单元格网格坐标 | 种子随机 | `initSectorRotation()` → `initMushroomCell()` |
| `monumentChunkX/Z` 等 | int | 三大结构（神殿/神庙/府邸）的强制 chunk 坐标 | 种子在对应区域选定 | 各自 `initXxx()` 懒初始化 |

### 4.2 缩写与命名建议

| 代码中缩写 | 实际含义 | 备注 |
|---|---|---|
| `islExt` / `ext` | islandSectorFalloff（群岛扇区 falloff） | 高频变量，命名可接受 |
| `islMask` | islandSectorMask（群岛扇区掩膜） | 高频变量，命名可接受 |
| `dd` | normalized distance（归一化距离，湖/岛半径） | **【建议重命名为：`normalizedDist`】** |
| `rn` | radial normalized（径向往 0~1 归一化） | **【建议重命名为：`radialNorm`】** |
| `angArch` | angle Arch（拱形角度 mask） | **【建议重命名为：`angleArchMask`】** |
| `w2` | weight2（第二段权重，离岸缓冲后小岛回升权重） | **【建议重命名为：`islandRiseWeight`】** |
| `val` | value（bias 值/地形值） | 局部变量，可接受 |
| `cfg` / `cfgCur` | Config（当前配置快照） | 局部变量，可接受 |
| `cx0/cz0` | cell X/Z（岛屿网格单元坐标） | 可接受 |
| `ox/oz` | offset X/Z（岛中心在单元格内的偏移位置） | 可接受 |

---

## 五、结构与特征

| 结构 | Java 文件 | 位置规则 | 特殊处理 |
|---|---|---|---|
| 海洋神殿 | `ContinentsIslandMonumentStructure` | 群岛扇区内部锚点 | 半径 300 格清空岛屿，强制深海；chunk spacing=1 强制命中 |
| 沙漠神庙 | `ContinentsIslandDesertPyramidStructure` | 沙漠扇区环带内随机点 | chunk spacing=1 |
| 沼泽小屋 | `ContinentsIslandSwampHutStructure` | 湿地浅滩带（0.80R~0.98R） | 群系湖若抽中沼泽群系，湖中心附近也强制一座 |
| 林地府邸 | `ContinentsIslandWoodlandMansionStructure` | 扇区间隙（非扇区非湖非群岛） | 周围 200 格强制黑森林群系，府邸周围保证有黑森林地形 |
| 环山矿物 | `RingMountainOreFeature` + JSON × 24 | 仅环形山脉 mask>0.02 区域 | 8 种矿物×3 尺寸（small/medium/large），放置范围/密度与原版矿石不同 |

---

## 六、已知坑点

1. **注册期 vs 运行期配置不同步**：密度函数构造函数在 `RegisterEvent` 阶段调用，此时 `CAIConfig` 尚未加载。任何构造函数中的配置读取都会崩溃。必须在 `compute()` 中走 `ensureConfigLoaded()` → 静态变量。

2. **湖与群岛扇区的判定顺序**：`bias()` 中湖必须**先于**群岛扇区判定。否则群岛扇区（角度随种子旋转）可能盖住湖0（固定 0° 方位），导致湖地形被群岛值覆盖、湖干涸。

3. **offset_mountain.json 的湖修正必须用 LERP**：ADD 模式（`base + -0.50*band`）下湖面中心深度不稳定，base 高则湖变浅；LERP 模式下湖面中心永远是 -0.50，岸边沙滩环稳定。

4. **沙滩带的 band 曲线必须连续**：`ArchipelagoTransition` 中 0.02~0.05 段的末端（ext=0.05）必须与 0.05~0.10 段的起点完全相同（=0.10），否则会有 1.0→0.10 的跳变造成水下断崖。

5. **build/clean 会删除 clientRunVmArgs.txt**：`gradlew clean` 后必须重新 `gradlew prepareClientRun` 才能启动客户端，否则报错找不到 VM 参数文件。

6. **移除密度函数类型后，对应 JSON 必须同时删除**：之前删除 `jungle_wetland` 注册但没删其 JSON，导致世界创建时加载 JSON 找不到类型崩溃。

7. **沙滩带在内外两侧的 falloff→格 映射不均匀**：大陆内侧（falloff 变化慢）和群岛外侧（falloff 变化快）相同 falloff 宽度对应的实际格数不同。沙滩 30 格的目标在群岛外侧约对应 falloff 0.05 宽度，在内侧约对应 2~3 倍实际宽度。若要两侧绝对宽度一致需分段设计不同 falloff 阈值。